# Environment file for sync-product-master
NODE_ENV=production
# Add database URLs, API keys, or SAP credentials here
MONGO_URI="mongodb://localhost:27017/db-name"

SAP_API_URL=https://sap.example.com/api
SAP_USERNAME=sap_user
SAP_PASSWORD=sap_password