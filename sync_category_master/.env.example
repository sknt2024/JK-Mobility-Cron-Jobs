# Environment file for sync-product-master
NODE_ENV=production
# Add database URLs, API keys, or SAP credentials here
MONGO_URI="mongodb://localhost:27017/fleet-prod"
SAP_API_KEY="replace_with_actual_key"
